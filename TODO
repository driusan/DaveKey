Fix Logout/Login
Fix refresh(?)
Show profiles
  -- own profile
  -- Clickable profile images for other users
  -- Clickable mentions (go to profile)
Clickable hashtags (expected behaviour?)
    - /api/notes/search-by-tag
    (or some other behaviour? /api/hashtags/users? /api/hashtags/show?) 
Show custom emojis (1.in MFM posts 2. in reactions)
    - extract from /api/meta (emojis field)
Touch to show thread
CWs when applicable
Finish MFM implementation
Handle video embedded, not just images
